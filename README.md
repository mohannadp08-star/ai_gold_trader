# 🤖 Auto AI Gold Trader

Auto AI Gold Trader هو نظام تداول ذكي للذهب مقابل الدولار (XAU/USD) يستخدم **الذكاء الاصطناعي والتحليل الكمي** لتقديم إشارات شراء وبيع دقيقة، مع إدارة رأس المال وتنبيهات Discord، ويمكن تطويره لاحقًا للتنفيذ الآلي على MT5.

---

## 📂 هيكل المشروع





ai_gold_trader/

│app.py → الصفحة الرئيسية مع الشارت، السعر، التوصية، Stop Loss / Take Profit، Discord alerts.

alerts.py → إرسال التنبيهات إلى Discord.

data_fetch.py → جلب بيانات الذهب من Yahoo Finance.

analysis.py → حساب المؤشرات (RSI, EMA…).

quant_features.py → ميزات Quant إضافية (Volatility, Momentum…).

ml_model.py → تدريب Random Forest.

decision_engine.py → اتخاذ القرار BUY / SELL / HOLD.

model_utils.py → (يمكن تركها فارغة الآن، مستقبليًا لحفظ LSTM/RF models).

backtester.py → (يمكن تركها فارغة الآن، مستقبليًا لاختبار الأداء).

requirements.txt → جميع المكتبات المطلوبة.

---

## 📄 شرح الملفات

### 1️⃣ `requirements.txt`
- يحتوي على المكتبات اللازمة لتشغيل النظام:
  - `streamlit` لإنشاء واجهة المستخدم
  - `pandas` و `numpy` لمعالجة البيانات
  - `scikit-learn` لتدريب Random Forest
  - `tensorflow` لتدريب LSTM
  - `yfinance` لجلب بيانات الذهب
  - `MetaTrader5` للتنفيذ التلقائي مستقبليًا
  - `requests` لإرسال إشعارات Discord

---

### 2️⃣ `data_fetch.py`
- جلب بيانات الذهب التاريخية من Yahoo Finance.
- يعطي بيانات `XAU` (سعر الإغلاق)، `High` و `Low`.
- هذه البيانات تستخدم كأساس لكل التحليلات والنماذج.

---

### 3️⃣ `analysis.py`
- حساب المؤشرات الفنية الأساسية:
  - **EMA20 و EMA50** لتحديد الترند
  - **RSI14** لتحديد مناطق التشبع الشرائي والبيعي
  - **Return** لحساب العوائد
  - **Unusual** لاكتشاف الحركات الشاذة (تلاعب محتمل)

---

### 4️⃣ `quant_features.py`
- إضافة ميزات Quant متقدمة:
  - `Return_5`: العائد خلال 5 فترات
  - `Volatility`: التقلبات
  - `Momentum`: الزخم
  - `Trend_EMA`: الفرق بين EMA20 و EMA50
  - `RSI_zone`: فئة RSI (-1، 0، 1)

---

### 5️⃣ `ml_model.py`
- تدريب **Random Forest Classifier** للتنبؤ باتجاه السعر (Up / Down).
- المدخلات: الميزات المضافة من `quant_features.py`.
- المخرجات: `Target` = 1 إذا السعر سيرتفع، 0 إذا سينخفض.

---

### 6️⃣ `lstm_model.py`
- تدريب **LSTM** لتوقع السعر المستقبلي للذهب.
- يستخدم Lookback 20 شمعه.
- يحول البيانات باستخدام **MinMaxScaler**.
- يعطي توقع السعر القادم بدقة عالية.

---

### 7️⃣ `confidence.py`
- حساب **نسبة الثقة** لإشارة التداول بناءً على:
  - Random Forest
  - LSTM
  - RSI
  - اكتشاف الحركات الشاذة
- يعطي درجة ثقة من 0 إلى 100%.

---

### 8️⃣ `risk_management.py`
- حساب **ATR** لتحديد تقلب السعر.
- تحديد **SL و TP ذكيين** بناءً على ATR.
- حساب **Lot Size** بحيث يخاطر النظام بنسبة 1% فقط من الحساب.

---

### 9️⃣ `model_utils.py`
- حفظ وتحميل النماذج بعد التدريب.
- دعم إعادة التدريب اليومي.
- يحتوي وظائف مساعدة لـ RF و LSTM.

---

### 🔟 `decision_engine.py`
- الدمج النهائي لإشارات RF + LSTM + RSI + Anomaly.
- يقرر `BUY` / `SELL` / `HOLD`.

---

### 1️⃣1️⃣ `backtester.py`
- تجربة الاستراتيجية على بيانات تاريخية.
- حساب Sharpe Ratio، Max Drawdown، Win Rate.
- يساعد على اختبار الربحية قبل التداول الحقيقي.

---

### 1️⃣2️⃣ `discord_alerts.py`
- إرسال إشعارات إلى Discord باستخدام Webhook عند الإشارات القوية.

---

### 1️⃣3️⃣ `mt5_executor.py`
- تنفيذ الصفقات على منصة MT5.
- يحتوي Placeholder حاليا، يمكن تطويره لاحقًا.

---

### 1️⃣4️⃣ `app.py`
- التطبيق الرئيسي للنظام باستخدام **Streamlit**.
- يعرض:
  - السعر الحالي
  - السعر المتوقع
  - نسبة الثقة
  - القرار النهائي (BUY/SELL/HOLD)
  - SL و TP
- يربط كل الملفات السابقة معًا.
- يرسل تنبيه Discord إذا كانت الثقة > 75%.

---

## ⚡ تشغيل النظام

1. تثبيت المكتبات:

```bash
pip install -r requirements.txt

ستظهر لك واجهة تفاعلية لكل تحليلات الذهب مع إشارات التداول.
النظام جاهز للتطوير لاحقًا للتنفيذ الآلي على MT5.
